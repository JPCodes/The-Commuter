
<%= render 'layouts/errors', object: @response %>

<%= form_for [@conversation, @response] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>
